# QuickPulse V2 - Environment Variables
# Copy this file to .env and fill in your credentials
# NEVER commit .env to version control!

# =============================================================================
# Kingdee K3Cloud API Credentials
# =============================================================================
# Get these from your Kingdee K3Cloud admin panel

KINGDEE_SERVER_URL=http://your-server.com:8200/k3cloud/
KINGDEE_ACCT_ID=your_account_id
KINGDEE_USER_NAME=your_username
KINGDEE_APP_ID=your_app_id
KINGDEE_APP_SEC=your_app_secret
KINGDEE_LCID=2052

# Network timeouts (seconds)
KINGDEE_CONNECT_TIMEOUT=15
KINGDEE_REQUEST_TIMEOUT=30

# =============================================================================
# CVM (Shared Aliyun ECS) — Deployment Server
# =============================================================================
# Host: 121.41.81.36 | User: root | OS: Ubuntu 22.04.5 LTS
# QuickPulse Prod: :8003 | Dev: :8004
CVM_HOST=121.41.81.36
CVM_USER=root
CVM_PASSWORD=your_cvm_password

# =============================================================================
# Auth Configuration
# =============================================================================
AUTH_SECRET_KEY=change-me-to-a-random-string
AUTH_PASSWORD=quickpulse
AUTH_TOKEN_EXPIRE_MINUTES=1440

# =============================================================================
# CORS (comma-separated origins, leave empty for same-origin only)
# =============================================================================
CORS_ALLOWED_ORIGINS=

# =============================================================================
# Optional: DeepSeek API (for AI Chat Sidebar)
# =============================================================================
# Get your API key from https://platform.deepseek.com/
# When configured, enables the chat sidebar on the dashboard.
# DEEPSEEK_API_KEY=sk-your-deepseek-api-key
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_MAX_TOKENS=1024
# DEEPSEEK_TEMPERATURE=0.3
# DEEPSEEK_TIMEOUT_SECONDS=30

# =============================================================================
# Optional: Qwen API (for AI Chat Sidebar — alternative to DeepSeek)
# =============================================================================
# Get your API key from https://dashscope.console.aliyun.com/
# Both providers can be enabled simultaneously — switch via the UI dropdown.
# QWEN_API_KEY=sk-your-dashscope-api-key
# QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# QWEN_MODEL=qwen-plus
# QWEN_MAX_TOKENS=1024
# QWEN_TEMPERATURE=0.3
# QWEN_TIMEOUT_SECONDS=30

# =============================================================================
# Optional: Agent Pipeline LLM (separate model for agent-enhanced chat)
# =============================================================================
# Uses a different model for the agent pipeline (/api/agent-chat/stream).
# Falls back to DEEPSEEK_* values if not set — so you only need these if
# you want the agent pipeline to use a different model (e.g., Qwen 3.5).
#
# Example: Use Qwen 3.5 for agents while keeping DeepSeek for simple chat:
#   AGENT_API_KEY=sk-your-dashscope-api-key
#   AGENT_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
#   AGENT_MODEL=qwen3.5-plus
#
# AGENT_API_KEY=
# AGENT_BASE_URL=
# AGENT_MODEL=
# AGENT_MAX_TOKENS=2048
# AGENT_TEMPERATURE=0.1
# AGENT_TIMEOUT_SECONDS=60
